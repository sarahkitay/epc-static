import type { ApplicationError } from './types';
import { SqError } from './types';
export declare class TokenizationError extends SqError {
    readonly errors: ApplicationError[];
    constructor(errors?: ApplicationError[]);
}
